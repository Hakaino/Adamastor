apply plugin: 'com.android.application'

android {
    signingConfigs {
        Adamastor {
            storeFile file('/home/wizzard/Documentos/Development/Segredos dos Antigos/Keys/adamastorKeystore.jks')
            keyAlias 'AdamastorKey'
            keyPassword 'Z4gFK30bt'
            storePassword 'Sk0PvADe'
        }
    }
    compileSdkVersion 26
    buildToolsVersion '26.0.2'
    defaultConfig {
        applicationId "antigos.segredos.adamastor"
        minSdkVersion 17
        targetSdkVersion 26
        versionCode 6
        versionName "Adamastor"
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:26.1.0'
    compile 'com.google.firebase:firebase-ads:11.8.0'
    compile 'com.google.android.gms:play-services-ads:11.8.0'
    testCompile 'junit:junit:4.12'
}

apply plugin: 'com.google.gms.google-services'